# 1. 拆分核心服务。

将`TileCoreServuce`拆分为多个子模块。包括：
- 核心调度器。
- 瓦片管理器。
- 尺寸管理器。
- 事件处理器。

## 1.1 核心调度器

关联**布局引擎**、**瓦片管理器**、**尺寸管理器**、**事件处理器**、**渲染交互端**，作为轻量的中央控制器。

## 1.2 瓦片管理器

负责管理瓦片相关的**生命周期**、**存储**、**交互**等等。可**跨平台**。

## 1.3 尺寸管理器

负责管理尺寸相关**修改**、**存储**、**测量**等等。可**跨平台**。

## 1.4 事件处理器

负责处理安卓相关的**事件**与**动画**输入，并通知**核心调度器**进行布局扰动操作。

## 1.5 渲染交互层

优化`TileView`与`TileLayout`以适配新架构。重构并优化`TileLayout`的`requestLayout`的拦截机制，并且在进行`onBindTileHolder`时处理异常，若发生异常，及时回退层级计数器。

# 2. Demo

为所有可能受影响的 Demo 进行适配。

# 3. 调试层

重构调试层，争取提升性能，并且采用更好的统计方式并修复BUG。

# 4. 注意事项

保持**布局引擎**与**布局模型**不变，它们是最新的，一切冲突以它们为准。
